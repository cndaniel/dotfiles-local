# use macvim 
alias vim='mvim -v'
alias m='mvim -v'

alias cp="cp -iv"
alias rm="rm -iv"
alias mv="mv -iv"
alias ls="ls -FGh"

# Use modern regexps for sed. i.e. "(one|two), not "\(one\|two\)"
alias sed="sed -E"

# Copy-pasting '$ python something.py' works
alias \$=''

